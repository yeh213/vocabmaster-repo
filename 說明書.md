# VocabMaster 英文單字練習軟體 — 使用說明書

## 目錄

1. [快速開始](#1-快速開始)
2. [檔案結構](#2-檔案結構)
3. [功能介紹](#3-功能介紹)
   - [📊 總覽](#-總覽)
   - [📝 我的單字](#-我的單字)
   - [🃏 單字卡](#-單字卡)
   - [🧪 測驗](#-測驗)
4. [字首分類](#4-字首分類)
5. [發音功能](#5-發音功能)
6. [本機 JSON 儲存](#6-本機-json-儲存)
7. [間隔重複演算法（SM-2）](#7-間隔重複演算法sm-2)
8. [常見問題](#8-常見問題)

---

## 1. 快速開始

**不需要安裝任何軟體。**

1. 下載或解壓縮資料夾，確認以下四個檔案在同一個資料夾內：
   ```
   index.html
   style.css
   app.js
   vocab-data.json
   ```
2. 用瀏覽器（Chrome 或 Edge）開啟 `index.html`
3. 點左側欄底部的「設定自動儲存」，選擇 `vocab-data.json`

> **建議瀏覽器**：Google Chrome 或 Microsoft Edge，語音功能效果最佳。

---

## 2. 檔案結構

```
資料夾/
├── index.html    ← 主程式入口，用瀏覽器開啟此檔案
├── style.css     ← 外觀樣式
├── app.js        ← 所有功能邏輯
├── vocab-data.json ← 單字資料檔
└── 說明書.md     ← 本說明文件
```

所有資料儲存於：
- **vocab-data.json**：建議主要資料檔，可透過「設定自動儲存」連結
- **瀏覽器 localStorage**：本機快取，未連結 JSON 檔時仍可暫存資料

---

## 3. 功能介紹

### 📊 總覽

開啟後的首頁，顯示整體學習狀況：

| 統計項目 | 說明 |
|----------|------|
| 總單字數 | 已新增的單字總數 |
| 已熟練 | 複習間隔超過 21 天的單字（代表長期記憶穩固）|
| 答題正確率 | 所有練習的累計答對率 |

下方另有**最近新增**與**群組統計**兩個表格。

---

### 📝 我的單字

管理所有單字的地方。

#### 新增單字

點右上角「＋ 新增單字」，填寫以下欄位：

| 欄位 | 必填 | 說明 |
|------|------|------|
| 英文單字 | ✅ | 要記憶的英文單字，例：`ambiguous` |
| 中文釋義 | ✅ | 中文解釋，例：`模糊的、不明確的` |
| 詞性 | 選填 | 例：`adj.; n.` |
| 例句 | 選填 | 包含該單字的英文例句 |
| 分類 | 自動 | 依英文單字第一個字母自動分類，例如 `commercial` 會歸到 `C` |

新增後資料會先儲存在本機；若已設定自動儲存，資料會同步寫入 `vocab-data.json`。資料格式支援多義項、多詞性與多例句，圖片匯入時可完整保存這些資訊。

#### 搜尋

在搜尋列輸入關鍵字，可同時搜尋英文單字與中文釋義。

#### 單字狀態

| 狀態 | 說明 |
|------|------|
| 新增 | 尚未進行任何練習 |
| 學習中 | 已複習，但複習間隔未達 21 天 |
| 已熟練 | 複習間隔超過 21 天，代表長期記憶穩固 |

#### 編輯與刪除

每列單字右側有「編輯」與「刪除」按鈕，刪除前會要求確認。

---

### 🃏 單字卡

#### 練習流程

```
① 選擇群組（若有建立群組）
        ↓
② 選擇練習模式
   ┌──────────────┐   ┌──────────────┐
   │  🃏 翻牌模式  │   │  ⌨️ 打字模式  │
   └──────────────┘   └──────────────┘
        ↓
③ 開始練習
```

#### 翻牌模式

- 正面顯示英文單字，點擊翻牌查看中文釋義
- 翻牌後可再次點擊翻回正面
- 翻牌後根據記憶程度自評：

| 按鈕 | 意義 | SM-2 效果 |
|------|------|-----------|
| 😰 不會 | 完全不記得 | 重置進度，明天再複習 |
| 🤔 模糊 | 有點印象但不確定 | 維持目前進度，近期再複習 |
| 😊 記得 | 清楚記得 | 延長下次複習間隔 |

#### 打字模式

- 顯示中文釋義，輸入對應的英文單字
- 按 `Enter` 或點「確認」送出
- 答對：綠色提示，自動播放發音，1.2 秒後進下一題
- 答錯：顯示正確答案，手動點「繼續 →」

### 🧪 測驗

提供兩種測驗模式，每次最多出 10 題，完成後顯示分數與正確率。

#### 選擇題

- 顯示英文單字，從 4 個選項中選出正確的中文釋義
- 選錯後顯示正確答案

#### 拼寫測驗

- 顯示中文釋義，輸入正確的英文單字
- 按 `Enter` 或點「確認」送出
- 可點「跳過」略過該題

---

## 4. 字首分類

單字會依照英文單字的第一個字母自動分類到 A-Z。

例如：

- `apple` → A
- `commercial` → C
- `zebra` → Z

#### 切換分類

單字列表上方有 A-Z 分類標籤，點擊可切換顯示特定字首的單字。

```
[全部 23] [A 8] [B 5] [C 10] ...
```

#### 練習時選擇分類

進入單字卡或測驗時，可選擇全部單字或特定字首分類：

```
[📚 全部]  [📁 A]  [📁 B]  [📁 C]
```

---

## 5. 發音功能

單字旁的 **🔊** 按鈕可播放英文發音（使用瀏覽器內建語音引擎，不需網路）。

| 出現位置 | 說明 |
|----------|------|
| 📝 我的單字 | 每個單字旁，隨時點擊 |
| 🃏 單字卡 | 正面與背面皆有 🔊，點擊不會誤觸翻牌 |
| ⌨️ 打字模式 | 答對時自動播放，答錯或跳過後可手動點擊 |

> Windows 上使用 Edge 或 Chrome，英文語音品質最佳。

---

## 6. 本機 JSON 儲存

本軟體改為純本機使用，不會連線到任何雲端服務。單字與群組可以透過 `vocab-data.json` 保存。

#### 設定自動儲存

點左側欄底部的「設定自動儲存」，選擇 `vocab-data.json`。授權後，以下操作會自動寫入 JSON 檔：

- 新增、編輯、刪除單字
- 複習評分（SM-2 進度更新）

#### 換電腦使用

將 `index.html`、`style.css`、`app.js`、`vocab-data.json` 一起複製到新電腦，再用瀏覽器開啟 `index.html` 即可。若瀏覽器要求授權，重新點「設定自動儲存」並選擇同一個 JSON 檔。

---

## 7. 間隔重複演算法（SM-2）

本軟體使用 **SM-2 演算法**（SuperMemo 2）自動排程複習時間，讓記憶效率最大化。

#### 核心概念

- 記得越牢，下次複習間隔越長（1天 → 6天 → 數週 → 數月）
- 忘記時，複習間隔重置，從頭開始
- 目標是在「快要忘記的時刻」複習，鞏固長期記憶

#### 單字進度

| 狀態 | 條件 |
|------|------|
| 新增 | 從未練習過 |
| 學習中 | 複習間隔 < 21 天 |
| 已熟練 | 複習間隔 ≥ 21 天 |

#### 評分對應

| 評分 | 翻牌按鈕 | 打字結果 | 效果 |
|------|----------|----------|------|
| 5（完全記得）| 😊 記得 | 答對 | 大幅延長間隔 |
| 3（模糊）| 🤔 模糊 | — | 小幅延長間隔 |
| 1（不記得）| 😰 不會 | 答錯／跳過 | 重置間隔為 1 天 |

---

## 8. 常見問題

**Q：單字資料會消失嗎？**
A：若已設定自動儲存，資料會寫入 `vocab-data.json`。如果尚未設定，資料只會暫存在目前瀏覽器的 localStorage，清除瀏覽器資料可能會遺失。

---

**Q：可以在手機上使用嗎？**
A：可以，用手機瀏覽器開啟 `index.html` 即可使用。側邊欄在小螢幕上會自動收合成圖示。

---

**Q：測驗說需要至少 4 個單字是什麼意思？**
A：選擇題需要 1 個正確選項 + 3 個干擾選項，因此至少要有 4 個單字才能出題。若使用群組篩選，該群組內也需要至少 4 個單字。

---

**Q：🔊 按鈕沒有聲音？**
A：請確認：
1. 電腦音量是否開啟
2. 瀏覽器是否為 Chrome 或 Edge（建議）
3. 部分瀏覽器需要使用者先與頁面互動（點擊任何地方）後才允許播放語音

---

**Q：換瀏覽器後看不到最新資料怎麼辦？**
A：在新瀏覽器中重新點「設定自動儲存」，選擇同一個 `vocab-data.json`。程式會讀取檔案中的單字資料。
